import { Routes } from '@angular/router';

// Esto archivo sirve para definir las rutas de la aplicación, solo se utilizaría si se quiere hacer una aplicación con varias páginas
export const routes: Routes = [];
